import { FlatmateNamePipe } from './flatmate-name.pipe';

describe('FlatmateNamePipe', () => {
  it('create an instance', () => {
    const pipe = new FlatmateNamePipe();
    expect(pipe).toBeTruthy();
  });
});
